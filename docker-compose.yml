services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: skins
      POSTGRES_PASSWORD: skins
      POSTGRES_DB: skins
    ports:
      - '5433:5432'
    volumes:
      - postgres:/var/lib/postgresql/data

volumes:
  postgres:
